# config for Travis-CI
# https://travis-ci.org/cgoldberg/ystockquote

language: python
python:
    - 2.7
    - 3.5
install: 
    pip install -q pep8
    pip install -q testscenarios
script:
    python -m unittest discover
